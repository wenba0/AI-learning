### Norm
batchNorm是计算一个batch中数据的均值与方差，LayerNorm是对单个数据的特征进行归一化，无本质差别
layernorm与rmsnorm
$LayerNorm(x) = \frac{x-\mu}{\sigma}*{\gamma}+{\beta}$，其中$\sigma$是标准差，方差计算公式为  $\sigma^2 = \frac{1}{N} \sum_{i=1}^{N} (x_i - \mu)^2$ 
$RMSNorm(x) = \frac{x}{RMS(x)}*g$，其中$RMS(x)$为均方根：$\text{RMS}(x) = \sqrt{\frac{1}{N} \sum_{i=1}^{N} x_i^2}$
结论：RMSNorm是非零均值的，只抑制了特征的大小，不强制转移到0均值，避免了梯度消失，同时较少了计算量，RMSNorm更优！
### MOE
以Qwen3-30B-A3B为例，大体推理流程与MOE模块如下所示:
- 对于输入的的input_ids，首先将其转化为embedding，再经过每一层的decoder layer，最后经过norm和lm_head得到下一个输出token的概率，再通过sampler提取token
- 对于每一层的decoder layer，里面就是正常的attention、norm等模块，MoE模型便是将原始的mlp替换为moe模块
- 对于moe模块，首先通过gate(linear)层获取所有experts的logits，输入2048输出128；结果通过softmax来获取每个expert的权重，并通过topk选出概率最大的top_k个expert；
- 遍历所有被选中的expert，对于每个expert，提取对应的hidden_states（current_state）送入expert层进行计算，输出结果与routing_weights进行点乘；初始化一个全0的final_hidden_states，将每个expert计算的结果add到对应位置，
![Qwen3-30B-A3B MOE架构|1125](../assets/moe.png)


dsv4
![[../assets/Pasted image 20260424161239.png]]
![[../assets/Pasted image 20260424161425.png]]
![[../assets/Pasted image 20260424161934.png]]
![[../assets/Pasted image 20260424162206.png]]
![[../assets/Pasted image 20260424162443.png]]
![[../assets/Pasted image 20260424162940.png]]
![[../assets/Pasted image 20260424163122.png]]
![[../assets/Pasted image 20260424163254.png]]
![[../assets/Pasted image 20260424163800.png]]
![[../assets/Pasted image 20260424164205.png]]
![[../assets/Pasted image 20260424164411.png]]
